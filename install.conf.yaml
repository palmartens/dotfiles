- clean: ['~']

- link:
    ~/.profile:
        path: profile
        force: true
    ~/.bashrc:
        path: bashrc
        force: true
    ~/.bash_logout:
        path: bash_logout
        force: true
    ~/.bash_aliases: bash_aliases
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true

- shell:
  - chmod 700 ~/.ssh && chmod 600 ~/.ssh/*
  - sudo apt install -y bat


